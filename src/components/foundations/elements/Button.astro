---
const {
  variant,
  size,
  onlyIconSize,
  iconOnly = false,
  gap,
  isLink = false,
  class: className,
  ...rest
} = Astro.props;
const defaultClass = [
  "text-white",
  "bg-base-950",
  "hover:bg-base-900",
  "focus:outline-base-300",
];
const mutedClass = [
  "text-base-950",
  "bg-base-200",
  "hover:bg-base-100",
  "focus:outline-base-300",
];
// Size
const sizes = {
  xxs: ["h-8", "px-4", "py-2", "text-xs"],
  xs: ["h-9.5", "px-4", "py-3", "text-xs"],
  sm: ["h-10", "px-4", "py-3", "text-sm"],
  base: ["h-10.5", "px-5", "py-4", "text-base"],
  md: ["h-11", "px-6", "py-4", "text-base"],
  lg: ["h-12", "px-6", "py-4", "text-lg"],
  xl: ["h-13", "px-6", "py-4", "text-lg"],
};
// Icon size
const iconSizes = {
  xxs: ["size-8", "py-2", "text-xs"],
  xs: ["size-9.5", "text-xs"],
  sm: ["size-10", "text-sm"],
  base: ["size-10.5", "text-base"],
  md: ["size-11", "text-base"],
  lg: ["size-12", "text-lg"],
  xl: ["size-13", "text-base"],
};
// Gaps
const gapMap = {
  xs: ["gap-2"],
  sm: ["gap-4"],
  base: ["gap-8"],
  md: ["gap-10"],
  lg: ["gap-12"],
};
const variantClass = {
  default: defaultClass,
  muted: mutedClass,
  none: [],
};
const sizeClass = iconOnly ? iconSizes[size] : sizes[size] || [];
const gapClass = !iconOnly && gap ? gapMap[gap] : [];
const additionalClasses = className ? className.split(" ") : [];
const baseClass = [
  "flex",
  "justify-center",
  "text-center",
  "rounded-full",
  "font-medium",
  "items-center",
  "duration-500",
  "ease-in-out",
  "transition-all",
  "focus:outline-2",
  "focus:outline-offset-4",
  "select-none",
];
---

{
  isLink ? (
    <a
      href={rest.href}
      class:list={[
        ...baseClass,
        ...(variantClass[variant] || []),
        ...sizeClass,
        ...gapClass,
        ...additionalClasses,
      ]}
      {...rest}
    >
      {iconOnly ? (
        <slot name="icon" />
      ) : (
        <>
          <slot name="left-icon" />
          <slot />
          <slot name="right-icon" />
        </>
      )}
    </a>
  ) : (
    <button
      class:list={[
        ...baseClass,
        ...(variantClass[variant] || []),
        ...sizeClass,
        ...gapClass,
        ...additionalClasses,
      ]}
      {...rest}
    >
      {iconOnly ? (
        <slot name="icon" />
      ) : (
        <>
          <slot name="left-icon" />
          <slot />
          <slot name="right-icon" />
        </>
      )}
    </button>
  )
}
